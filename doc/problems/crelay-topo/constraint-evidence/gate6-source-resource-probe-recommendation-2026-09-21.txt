Approved gate6/source resource probe -- 2026-09-21
====================================================
User approval: “approved,” following the documented proposal.

One standalone GRAPH probe from *t10-keeper-checkpoint*: nine checkpoints,
32 actions, SHA-256 21953443D71570B85CD2E5A76D124E8D2720C03EB6648F8693AF8CA9F82D578D.
Preserve that checkpoint and save a successful result separately.

(stage crelay-topo)
(ww-set *threads* 16)
(defparameter *t10-keeper-checkpoint*
  (import-search-checkpoint
    (merge-pathnames
      "doc/problems/crelay-topo/constraint-evidence/t10-keeper-checkpoint.txt"
      (asdf:system-source-directory :wouldwork))))
(ww-set *depth-cutoff* 12)
(defparameter *t10-gate6-source-checkpoint*
  (solve-subgoal *t10-keeper-checkpoint*
    (and (color repeater1 red) (open gate6))))

Capture:
(list :outcome *last-search-outcome*
      :cutoff *depth-cutoff* :threads *threads*
      :cutoff-truncated *depth-cutoff-truncated*
      :cutoff-hits *depth-cutoff-hits* :search-mode *tree-or-graph*
      :symmetry-pruning *symmetry-pruning*
      :min-steps-pruning *min-steps-pruning-enabled*
      :new-checkpoint-p
      (not (eq *t10-gate6-source-checkpoint* *t10-keeper-checkpoint*)))

SUCCESS interpretation committed before execution: the returned endpoint has
physical red repeater source power and physical gate6 open under the actual
settings.  Inspect both gate6 witnesses, source pairings, relay colors,
holdings, agent positions, and physical/recording gate states before deciding
whether it supports a switch2 continuation.  This does not establish location14,
switch2 access, gate7, gate8, a receiver route, LK5, or the final goal.

EXHAUSTION interpretation committed before execution: no realization of this
exact conjunction was found from this checkpoint within cutoff 12 under the
reported coverage and pruning.  It is a grade-3 bound only; it does not prove
stranding, refute a different resource assignment or source sequence, or
authorize a retry/deepening.
